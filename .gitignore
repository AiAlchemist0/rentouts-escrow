# Foundry
out/
cache/
broadcast/
# ens/ commits its Sepolia broadcast receipts as deployment proof (ens/.gitignore drops dry-runs)
!ens/broadcast/
lib/

# Env / secrets
.env
.env.*
!.env.example

# Node (frontend, added later)
node_modules/
dist/
